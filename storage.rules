rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Règle : Un utilisateur ne peut uploader/voir que ses propres photos dans son dossier
    match /users/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}